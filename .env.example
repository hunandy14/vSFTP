# vSFTP 連線設定範例
# 格式：host=<host>;user=<user>[;key=<keypath>][;port=<port>]
# key 可選：省略時自動搜尋 ~/.ssh/id_rsa, id_ecdsa, id_ed25519 等
# port 可選：預設 22
# 注意：欄位值不能包含分號（;）

# 最簡形式（自動找金鑰）
SFTP_CONNECTION=host=example.com;user=myuser

# 完整形式
# SFTP_CONNECTION=host=example.com;port=22;user=myuser;key=/path/to/private_key
