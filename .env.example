# vSFTP 連線設定範例
# 格式：HostName=<host>;User=<user>[;IdentityFile=<keypath>][;Port=<port>]
# 欄位名稱與 OpenSSH config 一致（大小寫不敏感）
# IdentityFile 可選：省略時自動搜尋 ~/.ssh/id_rsa, id_ecdsa, id_ed25519 等
# Port 可選：預設 22
# 注意：欄位值不能包含分號（;）

# 最簡形式（自動找金鑰）
SFTP_CONNECTION=HostName=example.com;User=myuser

# 完整形式
# SFTP_CONNECTION=HostName=example.com;Port=22;User=myuser;IdentityFile=/path/to/private_key
